# Структура модулей дашборда ReportIB

## 🏗️ Архитектурное решение

- **Название/описание:** Плоская структура модулей дашборда без вложенности
- **Дата/версия:** 2025-05-26 / v1.0
- **Инициатор/ответственный:** Архитектор проекта / AI-ассистент
- **Зона применения (модуль/файл):** Дашборд (`src/app/dashboard/`, `src/components/dashboard/`, `src/components/sidebar/`)
- **Суть решения (что и зачем):**
  Упрощена архитектура дашборда с полным удалением вложенности модулей и настроек. Все модули переведены на единый уровень навигации без вложенных разделов для повышения удобства пользования и упрощения маршрутизации.
- **Аргументация (почему так):**
  Плоская структура модулей упрощает навигацию, исключает ошибки 404 при переходах, делает интерфейс более интуитивным и соответствующим современным стандартам UX, уменьшает когнитивную нагрузку на пользователя.
- **Ссылка на документацию/Context7:** architecture-templates.md
- **Дата внедрения/актуальности:** 2025-05-26

---

## 📦 Зона ответственности: Структура маршрутизации

- **Модуль/файл:** `src/app/dashboard/`
- **Основная функция:** Маршрутизация и структура URL для модулей дашборда
- **Входные данные:** URL запросы пользователя
- **Выходные данные:** Соответствующие страницы и компоненты
- **Взаимодействие с другими модулями:** Взаимодействует с `DashboardContent` и модулями контента
- **Важные зависимости:** Next.js App Router
- **Best practice/стандарты:**
  - Каждый модуль представлен отдельной директорией на одном уровне
  - Путь к модулю имеет формат `/dashboard/[модуль]`
  - Страницы модулей пустые (возвращают null), фактический контент рендерится через `DashboardContent`
- **Ответственный:** Архитектор проекта / AI-ассистент

---

## 📦 Зона ответственности: Сайдбар и навигация

- **Модуль/файл:** `src/components/sidebar/Sidebar.tsx`
- **Основная функция:** Навигация между модулями дашборда
- **Входные данные:** Роль пользователя, текущий путь
- **Выходные данные:** Навигационное меню, события навигации
- **Взаимодействие с другими модулями:** Интегрируется с `DashboardContent`, обновляет URL
- **Важные зависимости:** Next.js Router, SPA-навигация
- **Best practice/стандарты:**
  - Навигационные пункты сгруппированы по ролям пользователей
  - Все пункты находятся на одном уровне без вложенности
  - Стиль меню: градиентный фон, закругленные кнопки, иконки
- **Ответственный:** Архитектор проекта / AI-ассистент

---

## 📋 Новая плоская структура модулей

```plaintext
/dashboard/
├── statistics/    - Активность
├── plans/         - Планы
├── tasks/         - Задачи
├── reports/       - Отчеты
├── kpi/           - KPI
├── department/    - Отдел (для начальников)
├── employees/     - Сотрудники (самостоятельный модуль)
├── companies/     - Предприятия (самостоятельный модуль)
└── coefficients/  - Коэффициенты (самостоятельный модуль)
```

## 🔄 Обработка маршрутов в DashboardContent

Компонент `DashboardContent` является центральным элементом для обработки и отображения контента модулей. Он анализирует текущий путь и рендерит соответствующий компонент:

```tsx
switch (currentSection) {
  case 'statistics':
    return <ActivityContent user={user} />;
  case 'plans':
    return <PlansContent user={user} currentPath={activePath} fetchPlanCounts={fetchPlanCounts} />;
  case 'tasks':
    return <WeeklyPlansTasksBoard />;
  case 'reports':
    return <ReportsContent user={user} />;
  case 'department':
    return <DepartmentContent user={user} />;
  case 'employees':
    return <EmployeesContent user={user} />;
  case 'companies':
    return <CompaniesContent user={user} />;
  case 'coefficients':
    return <CoefficientsContent user={user} />;
  case 'kpi':
    return <KPIContent />;
  default:
    return <DefaultContent user={user} />;
}
```

## ✅ Преимущества новой структуры

1. **Улучшенный UX**: Более логичная и понятная структура навигации
2. **Упрощение разработки**: Единый подход к маршрутизации для всех модулей
3. **Надежность**: Исключение ошибок 404 при навигации
4. **Масштабируемость**: Легкое добавление новых модулей без изменения общей архитектуры
5. **Единообразие**: Стандартизация доступа ко всем модулям через `/dashboard/[модуль]`
