# ReportIB - Система управления планами и отчетами

## 🚀 О проекте

ReportIB - это современная веб-платформа для управления бизнес-процессами, включающая планирование, контроль выполнения задач и аналитику.

## 📋 Основные возможности

- Управление годовыми, квартальными и недельными планами
- Отслеживание KPI и метрик
- Управление задачами и проектами
- Автоматизированная отчетность
- Интеграция с Microsoft 365

## 🛠 Технологии

- **Frontend**: Next.js, React, TypeScript, Tailwind CSS
- **Backend**: Next.js API Routes, Supabase
- **Аутентификация**: Microsoft 365 (Azure AD)
- **База данных**: PostgreSQL (через Supabase)
- **Развертывание**: Vercel

## 🚀 Быстрый старт

1. Установите зависимости:

   ```bash
   npm install
   ```

2. Настройте переменные окружения (скопируйте `.env.example` в `.env.local`)

3. Запустите проект:

   ```bash
   npm run dev
   ```

## 📁 Структура проекта

```text
src/
├── app/           # Маршруты приложения (App Router)
├── components/    # UI-компоненты
├── lib/          # Вспомогательные библиотеки
├── services/     # Бизнес-логика и API-сервисы
└── types/        # TypeScript типы
```

## 📚 Документация

Подробная документация доступна в директории [docs/](docs/):

- [Архитектура](docs/architecture/README.md)

## 📄 Лицензия

MIT
  - `v_annual_plans`
  - `v_quarterly_plans`
  - `v_weekly_plans`
- Создание/редактирование — только с использованием Supabase user_id.

**Ключевые компоненты:**
- `PlansContent.tsx` — главный контейнер для отображения и управления планами.
- Модальные окна для добавления/редактирования планов всегда получают user_id из Supabase.

**Best practice:**
- Не обращаться к таблицам Supabase напрямую — только через views.
- Всегда приводить departmentId к строке.
- Для отображения автора использовать поля из view (author_name, author_email).

**Типовые ошибки и их решение:**
- **План не создаётся:** Проверь, что передаётся корректный Supabase user_id.
- **Не отображается автор:** Проверь, что view возвращает нужные поля, а компонент их отображает.
- **Ошибка доступа к данным:** Проверь права пользователя и корректность SQL view.

---

### 3. Модуль “Сотрудники” (управление профилями)

**Архитектура:**
- Все операции с профилями — только через хранимую процедуру `upsert_user_profile`.
- Поиск сотрудников — только по email (email нельзя менять после создания).
- Статусы, роли, отделы — реализованы через enum/типизацию.

**Ключевые компоненты:**
- `EmployeeFormModal` — универсальное модальное окно для добавления/редактирования.
- Таблица сотрудников — группировка по отделам и статусам.

**Best practice:**
- Никогда не изменять email сотрудника после создания.
- Все статусы и роли — только из справочника.
- Для любого действия с профилем использовать Supabase user_id.

**Типовые ошибки и их решение:**
- **Сотрудник не находится:** Проверь email (всегда уникальный, поиск только по нему).
- **Статус не обновляется:** Проверь, что используется правильный enum и вызывается процедура upsert.
- **Ошибка при добавлении:** Проверь уникальность email и корректность всех обязательных полей.

---

### 4. Модуль “Задачи” (Task Management)

**Архитектура:**
- Все задачи привязываются к планам и сотрудникам только по Supabase user_id.
- Используются централизованные сервисы для работы с задачами (создание, обновление, удаление).

**Ключевые компоненты:**
- `WeeklyPlansTasksBoard` — доска задач для недельных планов.
- `AddTaskModal` — модальное окно для добавления задачи.

**Best practice:**
- Никогда не использовать Azure ID для связи задач с пользователем.
- Все запросы к данным — только через сервисы и Supabase views.
- Статусы задач и исполнители — только из справочников и профилей.

**Типовые ошибки и их решение:**
- **Задача не отображается:** Проверь связь с планом и user_id исполнителя.
- **Ошибка при создании:** Проверь, что все обязательные поля заполнены и нет дублирования user_id.
- **Статус задачи не меняется:** Проверь, что обновление идёт через сервис и view.

---

### Общие рекомендации для всех модулей

- Всегда сверяйся с локальной документацией и MEMORY.
- Не смешивай идентификаторы разных систем (только Supabase user_id для бизнес-логики).
- Все ошибки должны быть максимально информативны для пользователя.
- При любом сбое — сначала проверь, что нет “вечной загрузки”, затем ищи причину по шагам из этой инструкции.

---        