# Архітектура модуля звітності

> Дата створення: 2026-01-28
> Статус: Проектування

## 1. Огляд

Модуль звітності призначений для формування актів виконаних робіт з надання послуг SOC (Security Operations Center) для підприємств-замовників.

## 2. Типи звітів

### 2.1. Щомісячний звіт по підприємству

**Призначення:** Акт виконаних робіт для конкретного підприємства за місяць.

**Структура:**
- Період (місяць, рік)
- Підприємство (назва, інфраструктура)
- Перелік наданих послуг (з договору)
- Деталізація виконаних робіт
- Виконавці (список співробітників)
- Загальна кількість годин

**Джерела даних:**
```
companies → weekly_plan_companies → weekly_plans → weekly_tasks
                                         ↓
                                   quarterly_plans → processes
```

### 2.2. Щомісячний звіт по співробітнику

**Призначення:** Звіт про виконану роботу конкретним співробітником за місяць.

**Структура:**
- Період (місяць, рік)
- ПІБ співробітника, відділ
- Виконані задачі (групування по планах/процесах)
- Загальна кількість годин
- Розподіл по підприємствах

**Джерела даних:**
```
user_profiles → weekly_tasks → weekly_plans → processes
                                    ↓
                          weekly_plan_companies → companies
```

### 2.3. Квартальний звіт по планах

**Призначення:** Зведений звіт про виконання квартальних планів.

**Джерела даних:**
```
v_quarterly_reports (існуюче view)
```

## 3. Архітектура генерації звітів

### 3.1. Підхід: Процеси ІБ → Послуги договору

Замість створення окремого справочника послуг, використовуємо **маппінг процесів ІБ на послуги договору**. AI на основі:
- Назви процесу ІБ
- Опису задачі
- Контексту плану

...формує відповідний текст з правильною послугою згідно договору.

**Переваги:**
- Не потрібні нові таблиці в БД
- Використовуємо існуючу структуру (processes)
- AI гнучко інтерпретує контекст
- Легко оновлювати маппінг

### 3.2. Схема генерації

```
┌─────────────────────────────────────────────────────────────┐
│                    ГЕНЕРАЦІЯ ЗВІТУ                          │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  1. Вибір параметрів (UI)                                   │
│     ├─ Тип звіту (по підприємству / по співробітнику)      │
│     ├─ Період (місяць, рік)                                 │
│     └─ Фільтри (підприємство / співробітник)               │
│                                                             │
│  2. Збір даних (API)                                        │
│     ├─ Задачі за період                                     │
│     ├─ Плани та процеси                                     │
│     ├─ Інформація про підприємства                          │
│     └─ Інфраструктура (з company_infrastructure)            │
│                                                             │
│  3. Генерація тексту (AI)                                   │
│     ├─ Маппінг процес → послуга                             │
│     ├─ Формування опису робіт                               │
│     └─ Структурування за категоріями                        │
│                                                             │
│  4. Формування документу                                    │
│     ├─ Застосування шаблону Word                            │
│     ├─ Вставка згенерованого тексту                         │
│     └─ Експорт .docx                                        │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

## 4. Інтеграція з SharePoint

### 4.1. Структура папок

```
📁 Shared Documents/
└── 📁 Планы и отчёты/
    └── 📁 ProjIB/
        ├── 📁 Шаблони/                    ← Word шаблони звітів
        │   ├── Акт_місячний_підприємство.docx
        │   ├── Звіт_місячний_співробітник.docx
        │   └── Звіт_квартальний.docx
        │
        ├── 📁 Звіти/
        │   ├── 📁 По підприємствах/       ← Щомісячні акти по компаніях
        │   │   └── 📁 2026/
        │   │       ├── 📁 01_Січень/
        │   │       │   ├── 📁 АТБ Маркет/
        │   │       │   │   └── Акт_АТБ_Маркет_01-2026.docx
        │   │       │   └── 📁 Логістік Юніон/
        │   │       └── 📁 02_Лютий/
        │   │
        │   ├── 📁 По співробітниках/      ← Щомісячні звіти по співробітниках
        │   │   └── 📁 2026/
        │   │       └── 📁 01_Січень/
        │   │           ├── Звіт_Петренко_01-2026.docx
        │   │           └── ...
        │   │
        │   └── 📁 Квартальні/             ← Квартальні звіти
        │       └── 📁 2026/
        │           └── 📁 Q1/
        │
        ├── 📁 Завдання/                   ← Вкладення до задач (СЗ)
        │   └── 📁 2026/
        │       ├── 📁 01_Січень/
        │       │   ├── СЗ_123_2026.docx
        │       │   └── document.pdf
        │       └── 📁 02_Лютий/
        │
        └── 📁 Архів/                      ← Архів старих звітів
```

### 4.1.1. Конфігурація шляхів (sharepoint-service.ts)

```typescript
static readonly FOLDERS = {
  TEMPLATES: 'Планы и отчёты/ProjIB/Шаблони',
  REPORTS_COMPANY: 'Планы и отчёты/ProjIB/Звіти/По підприємствах',
  REPORTS_EMPLOYEE: 'Планы и отчёты/ProjIB/Звіти/По співробітниках',
  REPORTS_QUARTERLY: 'Планы и отчёты/ProjIB/Звіти/Квартальні',
  ARCHIVE: 'Планы и отчёты/ProjIB/Архів',
  TASK_ATTACHMENTS: 'Планы и отчёты/ProjIB/Завдання',
};
```

> **Важливо:** Graph API root вказує на `Shared Documents`, тому шлях не включає цей префікс.

### 4.1.2. Ліміти завантаження файлів

| Розмір файлу | Метод завантаження |
|--------------|-------------------|
| < 4 MB | Простий PUT запит |
| 4 MB - 250 MB | Upload Session (чанками по 10 MB) |
| > 250 MB | Не підтримується (ліміт SharePoint) |

```typescript
// Для великих файлів автоматично використовується upload session
await GraphSharePointService.uploadTaskAttachment(file, docNumber, taskId, date);
```

### 4.2. SharePoint Service

**Файл:** `src/services/graph/sharepoint-service.ts`

#### 4.2.1. Методи для звітів

```typescript
// Ініціалізація структури папок (один раз)
await GraphSharePointService.initializeProjIBStructure();

// Отримання шаблонів
const templates = await GraphSharePointService.getReportTemplates();

// Завантаження шаблону для редагування
const templateBuffer = await GraphSharePointService.downloadTemplate('Акт_місячний.docx');

// Збереження звіту по підприємству
await GraphSharePointService.uploadCompanyReport(2026, 1, 'АТБ Маркет', docxContent);

// Збереження звіту по співробітнику
await GraphSharePointService.uploadEmployeeReport(2026, 1, 'Петренко І.І.', docxContent);
```

#### 4.2.2. Методи для вкладень задач

```typescript
// Завантаження вкладення до задачі
// Шлях: Завдання/{year}/{month}_Місяць/filename
const result = await GraphSharePointService.uploadTaskAttachment(
  file,           // File object
  documentNumber, // Номер СЗ (необов'язково)
  taskId,         // ID задачі (необов'язково)
  completedAt     // Дата виконання для визначення папки
);

// Результат:
// { success: true, webUrl: 'https://...sharepoint.com/...' }

// Витягування тексту з Word документа (локальний файл)
const text = await GraphSharePointService.extractTextFromFile(file);

// Витягування тексту з Word документа (за URL в SharePoint)
const text = await GraphSharePointService.downloadAndExtractText(webUrl);
```

#### 4.2.3. Upload Session для великих файлів

Для файлів більше 4MB автоматично використовується upload session:

```typescript
// Внутрішній метод uploadLargeFile
// 1. Створює upload session: POST /createUploadSession
// 2. Завантажує чанками по 10MB: PUT з Content-Range
// 3. Отримує DriveItem після останнього чанка

// Приклад лога:
// [SharePoint] Файл doc.pdf більше 4MB (15.23MB), використовуємо upload session
// [SharePoint] Upload session створена, завантажуємо 15970304 байт чанками по 10MB
// [SharePoint] Завантажено 10485760/15970304 байт (66%)
// [SharePoint] Завантажено 15970304/15970304 байт (100%)
```

### 4.3. Workflow генерації звіту

```
┌─────────────────────────────────────────────────────────────┐
│  1. Користувач обирає параметри звіту                       │
│     (тип, період, підприємство/співробітник)                │
└────────────────────────┬────────────────────────────────────┘
                         ▼
┌─────────────────────────────────────────────────────────────┐
│  2. Завантаження шаблону з SharePoint                       │
│     GraphSharePointService.downloadTemplate()               │
└────────────────────────┬────────────────────────────────────┘
                         ▼
┌─────────────────────────────────────────────────────────────┐
│  3. Збір даних з БД                                         │
│     - Задачі за період                                      │
│     - Процеси ІБ                                            │
│     - Інформація про виконавців                             │
└────────────────────────┬────────────────────────────────────┘
                         ▼
┌─────────────────────────────────────────────────────────────┐
│  4. Генерація тексту через AI                               │
│     - Маппінг процесів на послуги                           │
│     - Формування структурованого опису                      │
└────────────────────────┬────────────────────────────────────┘
                         ▼
┌─────────────────────────────────────────────────────────────┐
│  5. Заповнення Word шаблону                                 │
│     - Вставка AI-тексту                                     │
│     - Заповнення полів (дата, підприємство, години)         │
└────────────────────────┬────────────────────────────────────┘
                         ▼
┌─────────────────────────────────────────────────────────────┐
│  6. Збереження в SharePoint                                 │
│     GraphSharePointService.uploadCompanyReport()            │
└─────────────────────────────────────────────────────────────┘
```

## 5. Технічна реалізація

### 5.1. API Endpoints

```typescript
// Отримання даних для звіту
GET /api/reports/monthly-company
  ?company_id=uuid
  &year=2026
  &month=1

GET /api/reports/monthly-employee
  ?user_id=uuid
  &year=2026
  &month=1

// Генерація Word документу
POST /api/reports/generate
  {
    type: 'monthly-company' | 'monthly-employee',
    params: { ... },
    format: 'docx'
  }
```

### 4.2. AI Integration

```typescript
// src/lib/ai/report-generator.ts
POST /api/ai/report
  {
    type: 'monthly-company',
    data: {
      company: { ... },
      tasks: [ ... ],
      processes: [ ... ]
    }
  }

// Response: згенерований текст звіту
```

### 4.3. Word Generation

**Бібліотека:** `docx` (npm)

```typescript
import { Document, Paragraph, TextRun } from 'docx';

const doc = new Document({
  sections: [{
    properties: {},
    children: [
      new Paragraph({
        children: [new TextRun({ text: aiGeneratedText })]
      })
    ]
  }]
});
```

## 5. UI Components

### 5.1. ReportsContent (оновлення)

**Вкладки:**
1. **По підприємствах** - вибір компанії, місяця → генерація акту
2. **По співробітниках** - вибір співробітника, місяця → генерація звіту
3. **Квартальні** - існуючий функціонал (QuarterlyReportCard)

### 5.2. ReportGenerator Component

```typescript
interface ReportGeneratorProps {
  type: 'company' | 'employee';
  onGenerate: (params: ReportParams) => void;
}
```

## 6. Зв'язок з існуючою структурою

### 6.1. Таблиці що використовуються

| Таблиця | Призначення в звітах |
|---------|---------------------|
| `companies` | Список підприємств |
| `company_infrastructure` | Дані про інфраструктуру (серв./РС) |
| `weekly_plan_companies` | Зв'язок план-підприємство |
| `weekly_plans` | Плани з процесами |
| `weekly_tasks` | Виконані задачі |
| `quarterly_plans` | Квартальні плани |
| `processes` | Процеси ІБ (для маппінгу на послуги) |
| `user_profiles` | Співробітники |

### 6.2. Views для звітів (потрібно створити)

```sql
-- v_monthly_company_tasks
-- Задачі по підприємству за місяць

-- v_monthly_employee_tasks
-- Задачі по співробітнику за місяць
```

## 7. Маппінг процесів на послуги

Детальний маппінг процесів ІБ на послуги договору див.:
- [AI_REPORT_INSTRUCTIONS.md](./AI_REPORT_INSTRUCTIONS.md)

## 8. План реалізації

| Етап | Опис | Пріоритет |
|------|------|-----------|
| 1 | Створити SQL views для звітів | High |
| 2 | Створити API endpoints | High |
| 3 | Оновити UI ReportsContent | High |
| 4 | Інтегрувати AI для генерації тексту | Medium |
| 5 | Додати експорт в Word | Medium |

## 9. Приклад згенерованого звіту

```
ДОДАТОК ДО АКТУ № ___ від ___.___._____

Перелік послуг що надає Виконавець:

1. Забезпечення кібербезпеки інформаційно-комунікаційних систем:
   - узгодження надання доступу до ІС (45 операцій)
   - моніторинг подій антивірусного ПЗ (постійно)
   - контроль резервного копіювання (щоденно)

2. Розроблення та впровадження організаційних заходів:
   - моніторинг подій інформаційної безпеки (156 годин)
   - аналіз ризиків інформаційної безпеки (12 годин)

...

Загальна кількість годин: 168
Виконавці: Петренко І.І., Сидоренко О.В.
```
