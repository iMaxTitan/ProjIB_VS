# Как использовать AI инструкции для UI Design System

## 📁 Созданные файлы

1. **`.cursorrules`** - Инструкции для Cursor AI
2. **`CLAUDE.md`** - Инструкции для Claude AI
3. **`docs/UI_CHEATSHEET.md`** - Краткая шпаргалка
4. **`docs/UI_DESIGN_SYSTEM.md`** - Полная документация

## 🤖 Как настроить разные AI ассистенты

### Cursor AI
✅ **Уже готово!** Файл `.cursorrules` автоматически используется Cursor.

Просто откройте проект в Cursor, и AI будет следовать правилам автоматически.

### Claude (Desktop/Web)
1. Откройте файл `CLAUDE.md`
2. Скопируйте его содержимое
3. В начале разговора с Claude вставьте:
   ```
   Пожалуйста, следуй инструкциям из этого файла при работе с проектом:
   [вставить содержимое CLAUDE.md]
   ```

**Или лучше:**
- Положите `CLAUDE.md` в корень проекта
- Claude Code CLI автоматически прочитает его при работе с проектом

### GitHub Copilot
К сожалению, Copilot не поддерживает кастомные инструкции на уровне проекта.

**Альтернатива:**
- Используйте комментарии в начале файлов:
```tsx
/**
 * UI Guidelines:
 * - Всегда используй responsive классы (sm:, md:, lg:)
 * - Добавляй aria-label на все интерактивные элементы
 * - Используй gradient-* классы для градиентов
 * - Используй Button/Modal компоненты
 */
```

### Windsurf AI
Создайте файл `.windsurfrules` (аналогично `.cursorrules`):
```bash
cp .cursorrules .windsurfrules
```

### ChatGPT / Custom GPT
В начале сессии отправьте:
```
Я работаю над проектом с определенными UI правилами.
Пожалуйста, следуй этим инструкциям:

[вставить содержимое CLAUDE.md]
```

## 🎯 Как это работает

### Автоматически (Cursor, Claude Code)
AI видит `.cursorrules` или `CLAUDE.md` и автоматически применяет правила.

### Вручную (ChatGPT, Claude Web)
Вам нужно один раз в начале сессии вставить инструкции.

## 📝 Примеры использования

### Пример 1: Создание новой кнопки
**Вы пишете:**
```
Создай кнопку для добавления плана
```

**AI с инструкциями ответит:**
```tsx
<Button
  onClick={handleAddPlan}
  aria-label="Добавить новый план"
  className="gap-2"
>
  <Plus aria-hidden="true" className="h-4 w-4" />
  <span className="hidden xs:inline">Добавить план</span>
  <span className="xs:hidden">Добавить</span>
</Button>
```

**AI без инструкций:**
```tsx
<button onClick={handleAddPlan} className="bg-blue-500 text-white px-4 py-2 rounded">
  <Plus /> Добавить план
</button>
```

### Пример 2: Создание карточки
**Вы пишете:**
```
Создай карточку для отображения плана
```

**AI с инструкциями:**
```tsx
<div className="card-base card-hover p-4 sm:p-6">
  <h3 className="text-base sm:text-lg font-semibold mb-2 sm:mb-3">
    {plan.title}
  </h3>
  <p className="text-sm sm:text-base text-gray-600">
    {plan.description}
  </p>
</div>
```

### Пример 3: Рефакторинг
**Вы пишете:**
```
Улучши этот код:
<button onClick={handle} className="bg-blue-500 px-4 py-2">
  <Icon /> Text
</button>
```

**AI с инструкциями:**
```tsx
<Button
  onClick={handle}
  aria-label="Выполнить действие"
  className="gap-2"
>
  <Icon aria-hidden="true" className="h-4 w-4" />
  <span className="hidden xs:inline">Text</span>
</Button>
```

## 🔄 Обновление инструкций

Если вы измените систему дизайна:

1. Обновите `src/styles/design-tokens.ts`
2. Обновите `docs/UI_DESIGN_SYSTEM.md`
3. Обновите `.cursorrules` и `CLAUDE.md`
4. Перезапустите AI ассистент

## 📚 Быстрые ссылки

- **Полная документация:** [docs/UI_DESIGN_SYSTEM.md](UI_DESIGN_SYSTEM.md)
- **Шпаргалка:** [docs/UI_CHEATSHEET.md](UI_CHEATSHEET.md)
- **Design Tokens:** [src/styles/design-tokens.ts](../src/styles/design-tokens.ts)

## 💡 Советы

### 1. Будьте конкретны
❌ "Создай кнопку"
✅ "Создай кнопку для удаления с подтверждением"

### 2. Упоминайте компоненты
❌ "Создай модальное окно"
✅ "Создай модальное окно используя компонент Modal"

### 3. Просите про accessibility
❌ "Добавь навигацию"
✅ "Добавь навигацию с полной accessibility поддержкой"

### 4. Указывайте responsive требования
❌ "Сделай карточку"
✅ "Сделай адаптивную карточку для мобильных и десктопа"

## 🎯 Проверка работы инструкций

Попросите AI:
```
Создай простую кнопку для создания плана
```

Если AI ответит с:
- ✅ `aria-label`
- ✅ responsive классами (`sm:`, `md:`)
- ✅ `focus-ring` или focus states
- ✅ использованием `Button` компонента
- ✅ `aria-hidden="true"` на иконке

То инструкции работают правильно! 🎉

Если нет - убедитесь, что:
1. Файл `.cursorrules` или `CLAUDE.md` в корне проекта
2. AI ассистент перезапущен
3. Для ручных AI (ChatGPT) - инструкции вставлены в начале сессии

## 🆘 Проблемы?

### AI не следует правилам
- Убедитесь, что файл `.cursorrules` существует
- Перезапустите AI ассистент
- Для ручных AI - вставьте инструкции явно

### AI создает код не по шаблону
- Напомните: "Используй инструкции из CLAUDE.md"
- Явно укажите: "С полной accessibility" или "С responsive дизайном"

### Нужно изменить правила
1. Отредактируйте `.cursorrules` или `CLAUDE.md`
2. Перезапустите AI
3. Или просто скажите AI: "Игнорируй предыдущее правило о X, теперь делай Y"

---

**Главное:** С этими инструкциями AI будет создавать **консистентный**, **адаптивный** и **доступный** UI код автоматически! 🚀
