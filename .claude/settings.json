{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "bash -c 'if echo \"$CLAUDE_FILE_PATHS\" | grep -qE \"\\.(ts|tsx)$\"; then cd /c/Proj/ProjIB_VS && npx tsc --noEmit --pretty 2>&1 | head -30; fi'"
          }
        ]
      }
    ]
  }
}
