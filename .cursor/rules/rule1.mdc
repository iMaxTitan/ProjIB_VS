---
description: 
globs: 
alwaysApply: true
---
✅ Соблюдай следующие архитектурные правила:

1. **Логика отделена от UI**:
   - Компоненты в `src/components/` — только визуальные
   - Логика загрузки данных, роутинг и авторизация — в `src/app/...`

2. **Файловая структура строго соблюдается**:
   - `src/components/layout/` — layout-компоненты (`DashboardLayout`)
   - `src/components/sidebar/` — меню, элементы навигации
   - `src/components/dashboard/` — дашборд по ролям
   - `lib/` — Supabase, утилиты
   - `types/` — роли, типы задач, планы, отчёты

3. **Типизация строгая**, роль пользователя определяется через Supabase (`user_profiles`), тип `UserRole = 'chief' | 'head' | 'employee'`.

4. **Компоненты должны быть адаптивными**, использовать `flex`, `grid`, `max-w-screen-xl`, `gap`, `rounded`, `shadow`.

5. **Каждая роль видит свою часть интерфейса** — это реализуется через компонент `ContentByRole`, который принимает `role` и рендерит контент.

📁 Дополнительно:
- логика ролей, процессов, ограничения в `reportib_business_spec.md`
- структура БД, права, Supabase, RLS в `reportib_technical_spec.md`
- объединение бизнес + техническая модель в `reportib_spec.md` 


🎯 Всегда следи за чистотой кода и соблюдением архитектуры!
